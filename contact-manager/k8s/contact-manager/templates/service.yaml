apiVersion: v1
kind: Service
metadata:
  name: {{ include "contact-manager.fullname" . }}
  labels:
    {{- include "contact-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "contact-manager.selectorLabels" . | nindent 4 }}
