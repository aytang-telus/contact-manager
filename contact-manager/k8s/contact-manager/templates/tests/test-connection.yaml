apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "contact-manager.fullname" . }}-test-connection"
  labels:
    {{- include "contact-manager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "contact-manager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
